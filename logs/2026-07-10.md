# Day 12 · Friday, July 10, 2026

> Auto-generated by GitHub Actions. Random pick from a curated pool of 20 challenges and 20 tips.

---

## 🧠 Challenge of the Day

### Valid Parentheses

**Difficulty:** Easy
**Topic:** Stacks

**Problem:**

Given a string containing just the characters `(`, `)`, `{`, `}`, `[`, `]`, determine if the input string is valid. Open brackets must be closed by the same type, in the correct order.

**Example:**

```
Input: "()[]{}"
Output: true

Input: "([)]"
Output: false
```

**Hint:**

Use a stack. Push opening brackets, pop when you see a closing bracket and check it matches.

<details>
<summary>🔍 Reveal solution</summary>

```javascript
function isValid(s) {
  const stack = [];
  const pairs = { ')': '(', '}': '{', ']': '[' };
  for (const ch of s) {
    if (ch === '(' || ch === '{' || ch === '[') {
      stack.push(ch);
    } else {
      if (stack.pop() !== pairs[ch]) return false;
    }
  }
  return stack.length === 0;
}
```

**Explanation:** Stacks are perfect for nested/matching problems. Last-opened bracket must be first-closed — LIFO.

</details>

---

## 💡 Tip of the Day

**Destructure with default values and renames**

You can provide defaults AND rename in one go:

```js
const { name: userName = 'Anonymous', age = 18 } = user;
// userName = user.name (or 'Anonymous')
// age = user.age (or 18)
```

Also works with function params:
```js
function greet({ name = 'Anonymous' } = {}) { ... }
```

---

## 🎯 Today's Reflection

- [ ] I attempted the challenge before looking at the solution
- [ ] I learned something new from the tip
- [ ] I worked on a portfolio project today
- [ ] I read at least one page of official docs

**What I learned today:**

_Today I focused on..._

**Tomorrow I want to explore:**

_Next I want to look into..._

---

## 📚 Currently Building

Working through a 14-day portfolio build plan. Today is **Day 12**.

| Day | Project | Status |
|-----|---------|--------|
| 1 | Calculator App, Notes App, Realtime Chat | ✅ Done |
| 2 | Weather App | ✅ Done |
| 3 | Todo List with Drag & Drop | ✅ Done |
| 4 | Movie Explorer | ✅ Done |
| 5 | Pomodoro Timer + Stats | ✅ Done |
| 6 | GitHub User Search | ✅ Done |
| 7 | React Todo (Vite) | ✅ Done |
| 8 | URL Shortener API | ✅ Done |
| 9 | Markdown Blog | ✅ Done |
| 10 | Expense Tracker | ✅ Done |
| 11 | Real-time Whiteboard | ✅ Done |
| 12 | JWT Auth Demo | ✅ Done |
| 13 | AI Chatbot UI | ⏳ Pending |
| 14 | Portfolio Dashboard | ⏳ Pending |

---

_Hit me with a PR if you spot a bug or have a better solution — happy to learn from you!_ 🙌
