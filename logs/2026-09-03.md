# Day 67 · Thursday, September 3, 2026

> Auto-generated by GitHub Actions. Random pick from a curated pool of 20 challenges and 20 tips.

---

## 🧠 Challenge of the Day

### Palindrome Check

**Difficulty:** Easy
**Topic:** Strings

**Problem:**

Check if a string is a palindrome, ignoring case, spaces, and non-alphanumeric characters.

**Example:**

```
Input: "A man, a plan, a canal: Panama"
Output: true
```

**Hint:**

Sanitize the string first (lowercase, strip non-alphanumeric), then use two pointers from both ends.

<details>
<summary>🔍 Reveal solution</summary>

```javascript
function isPalindrome(str) {
  const clean = str.toLowerCase().replace(/[^a-z0-9]/g, '');
  let left = 0, right = clean.length - 1;
  while (left < right) {
    if (clean[left] !== clean[right]) return false;
    left++;
    right--;
  }
  return true;
}
```

**Explanation:** Cleaning the string first makes the comparison straightforward. Two-pointer avoids creating a reversed copy.

</details>

---

## 💡 Tip of the Day

**Avoid `forEach` when you need to `break` or `return`**

`forEach` doesn't support `break` or early return — use `for...of` instead:

```js
// ❌ Can't break out of forEach
arr.forEach(item => {
  if (item === target) return;  // doesn't do what you think!
  console.log(item);
});

// ✅ for...of supports break
for (const item of arr) {
  if (item === target) break;
  console.log(item);
}
```

`.some()` and `.every()` also short-circuit — useful for early-exit searches.

---

## 🎯 Today's Reflection

- [ ] I attempted the challenge before looking at the solution
- [ ] I learned something new from the tip
- [ ] I worked on a portfolio project today
- [ ] I read at least one page of official docs

**What I learned today:**

_Today I focused on..._

**Tomorrow I want to explore:**

_Next I want to look into..._

---

## 📚 Currently Building

Working through a 14-day portfolio build plan. Today is **Day 67**.

| Day | Project | Status |
|-----|---------|--------|
| 1 | Calculator App, Notes App, Realtime Chat | ✅ Done |
| 2 | Weather App | ✅ Done |
| 3 | Todo List with Drag & Drop | ✅ Done |
| 4 | Movie Explorer | ✅ Done |
| 5 | Pomodoro Timer + Stats | ✅ Done |
| 6 | GitHub User Search | ✅ Done |
| 7 | React Todo (Vite) | ✅ Done |
| 8 | URL Shortener API | ✅ Done |
| 9 | Markdown Blog | ✅ Done |
| 10 | Expense Tracker | ✅ Done |
| 11 | Real-time Whiteboard | ✅ Done |
| 12 | JWT Auth Demo | ✅ Done |
| 13 | AI Chatbot UI | ✅ Done |
| 14 | Portfolio Dashboard | ✅ Done |

---

_Hit me with a PR if you spot a bug or have a better solution — happy to learn from you!_ 🙌
