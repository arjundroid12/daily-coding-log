# Day 39 · Thursday, August 6, 2026

> Auto-generated by GitHub Actions. Random pick from a curated pool of 20 challenges and 20 tips.

---

## 🧠 Challenge of the Day

### Merge Two Sorted Arrays

**Difficulty:** Medium
**Topic:** Arrays / Two Pointers

**Problem:**

Given two sorted arrays `a` and `b`, merge them into a single sorted array.

**Example:**

```
Input: a = [1, 3, 5], b = [2, 4, 6]
Output: [1, 2, 3, 4, 5, 6]
```

**Hint:**

Use two pointers — one per array. Compare current elements, push the smaller one, advance that pointer.

<details>
<summary>🔍 Reveal solution</summary>

```javascript
function mergeSorted(a, b) {
  const result = [];
  let i = 0, j = 0;
  while (i < a.length && j < b.length) {
    if (a[i] <= b[j]) result.push(a[i++]);
    else result.push(b[j++]);
  }
  while (i < a.length) result.push(a[i++]);
  while (j < b.length) result.push(b[j++]);
  return result;
}

// O(n + m) time
```

**Explanation:** Exploits the fact that inputs are already sorted. This is the merge step of merge sort.

</details>

---

## 💡 Tip of the Day

**Use `Intl` APIs for locale-aware formatting**

Stop hardcoding date/number formats. Use `Intl`:

```js
// Currency
new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' })
  .format(1234567.89);  // '₹12,34,567.89' (Indian grouping)

// Dates
new Intl.DateTimeFormat('en-IN', { dateStyle: 'full' })
  .format(new Date());  // 'Monday, 29 June 2026'

// Relative time
new Intl.RelativeTimeFormat('en', { style: 'short' })
  .format(-1, 'day');  // 'yesterday'
```

---

## 🎯 Today's Reflection

- [ ] I attempted the challenge before looking at the solution
- [ ] I learned something new from the tip
- [ ] I worked on a portfolio project today
- [ ] I read at least one page of official docs

**What I learned today:**

_Today I focused on..._

**Tomorrow I want to explore:**

_Next I want to look into..._

---

## 📚 Currently Building

Working through a 14-day portfolio build plan. Today is **Day 39**.

| Day | Project | Status |
|-----|---------|--------|
| 1 | Calculator App, Notes App, Realtime Chat | ✅ Done |
| 2 | Weather App | ✅ Done |
| 3 | Todo List with Drag & Drop | ✅ Done |
| 4 | Movie Explorer | ✅ Done |
| 5 | Pomodoro Timer + Stats | ✅ Done |
| 6 | GitHub User Search | ✅ Done |
| 7 | React Todo (Vite) | ✅ Done |
| 8 | URL Shortener API | ✅ Done |
| 9 | Markdown Blog | ✅ Done |
| 10 | Expense Tracker | ✅ Done |
| 11 | Real-time Whiteboard | ✅ Done |
| 12 | JWT Auth Demo | ✅ Done |
| 13 | AI Chatbot UI | ✅ Done |
| 14 | Portfolio Dashboard | ✅ Done |

---

_Hit me with a PR if you spot a bug or have a better solution — happy to learn from you!_ 🙌
