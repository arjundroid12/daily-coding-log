# Day 76 · Saturday, September 12, 2026

> Auto-generated by GitHub Actions. Random pick from a curated pool of 20 challenges and 20 tips.

---

## 🧠 Challenge of the Day

### Sum of Digits

**Difficulty:** Easy
**Topic:** Math / Loops

**Problem:**

Given a non-negative integer, return the sum of its digits.

**Example:**

```
Input: 12345
Output: 15  (1 + 2 + 3 + 4 + 5)
```

**Hint:**

Either convert to string and iterate, or repeatedly mod 10 + divide by 10.

<details>
<summary>🔍 Reveal solution</summary>

```javascript
// String approach
function sumDigits(n) {
  return String(n).split('').reduce((sum, d) => sum + Number(d), 0);
}

// Math approach (no string conversion)
function sumDigitsMath(n) {
  let sum = 0;
  while (n > 0) {
    sum += n % 10;
    n = Math.floor(n / 10);
  }
  return sum;
}
```

**Explanation:** The math approach is faster (no string allocation) and works in any language. String approach is more readable.

</details>

---

## 💡 Tip of the Day

**Use `Set` for fast membership checks**

`Set.has()` is O(1), `Array.includes()` is O(n).

```js
// ❌ Slow for large arrays
const allowed = ['admin', 'editor', 'moderator'];
if (allowed.includes(role)) { ... }

// ✅ Fast — O(1) lookup
const allowed = new Set(['admin', 'editor', 'moderator']);
if (allowed.has(role)) { ... }
```

Rule of thumb: if you call `.includes()` more than once on the same array, convert to Set.

---

## 🎯 Today's Reflection

- [ ] I attempted the challenge before looking at the solution
- [ ] I learned something new from the tip
- [ ] I worked on a portfolio project today
- [ ] I read at least one page of official docs

**What I learned today:**

_Today I focused on..._

**Tomorrow I want to explore:**

_Next I want to look into..._

---

## 📚 Currently Building

Working through a 14-day portfolio build plan. Today is **Day 76**.

| Day | Project | Status |
|-----|---------|--------|
| 1 | Calculator App, Notes App, Realtime Chat | ✅ Done |
| 2 | Weather App | ✅ Done |
| 3 | Todo List with Drag & Drop | ✅ Done |
| 4 | Movie Explorer | ✅ Done |
| 5 | Pomodoro Timer + Stats | ✅ Done |
| 6 | GitHub User Search | ✅ Done |
| 7 | React Todo (Vite) | ✅ Done |
| 8 | URL Shortener API | ✅ Done |
| 9 | Markdown Blog | ✅ Done |
| 10 | Expense Tracker | ✅ Done |
| 11 | Real-time Whiteboard | ✅ Done |
| 12 | JWT Auth Demo | ✅ Done |
| 13 | AI Chatbot UI | ✅ Done |
| 14 | Portfolio Dashboard | ✅ Done |

---

_Hit me with a PR if you spot a bug or have a better solution — happy to learn from you!_ 🙌
