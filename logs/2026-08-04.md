# Day 37 · Tuesday, August 4, 2026

> Auto-generated by GitHub Actions. Random pick from a curated pool of 20 challenges and 20 tips.

---

## 🧠 Challenge of the Day

### Anagram Check

**Difficulty:** Easy
**Topic:** Strings / Sorting

**Problem:**

Check if two strings are anagrams (contain the same characters with the same frequencies).

**Example:**

```
Input: "listen", "silent"
Output: true

Input: "hello", "world"
Output: false
```

**Hint:**

Sort both strings and compare. Or, count character frequencies with a Map.

<details>
<summary>🔍 Reveal solution</summary>

```javascript
// Approach 1: Sort and compare (concise)
function isAnagram(a, b) {
  const norm = (s) => s.toLowerCase().split('').sort().join('');
  return norm(a) === norm(b);
}

// Approach 2: Frequency counter (faster for long strings)
function isAnagramFast(a, b) {
  if (a.length !== b.length) return false;
  const counts = new Map();
  for (const ch of a.toLowerCase()) {
    counts.set(ch, (counts.get(ch) || 0) + 1);
  }
  for (const ch of b.toLowerCase()) {
    if (!counts.has(ch)) return false;
    counts.set(ch, counts.get(ch) - 1);
    if (counts.get(ch) < 0) return false;
  }
  return true;
}
```

**Explanation:** Sorting is O(n log n) but very concise. Frequency counting is O(n) but more code. Pick based on input size.

</details>

---

## 💡 Tip of the Day

**Use `??=` and `||=` for conditional assignment**

ES2021 added compound assignment for nullish and logical OR:

```js
// Old way
if (obj.prop == null) obj.prop = 'default';

// New way
obj.prop ??= 'default';  // assign only if null/undefined
obj.count ||= 1;         // assign only if falsy
obj.active &&= false;    // assign only if truthy
```

---

## 🎯 Today's Reflection

- [ ] I attempted the challenge before looking at the solution
- [ ] I learned something new from the tip
- [ ] I worked on a portfolio project today
- [ ] I read at least one page of official docs

**What I learned today:**

_Today I focused on..._

**Tomorrow I want to explore:**

_Next I want to look into..._

---

## 📚 Currently Building

Working through a 14-day portfolio build plan. Today is **Day 37**.

| Day | Project | Status |
|-----|---------|--------|
| 1 | Calculator App, Notes App, Realtime Chat | ✅ Done |
| 2 | Weather App | ✅ Done |
| 3 | Todo List with Drag & Drop | ✅ Done |
| 4 | Movie Explorer | ✅ Done |
| 5 | Pomodoro Timer + Stats | ✅ Done |
| 6 | GitHub User Search | ✅ Done |
| 7 | React Todo (Vite) | ✅ Done |
| 8 | URL Shortener API | ✅ Done |
| 9 | Markdown Blog | ✅ Done |
| 10 | Expense Tracker | ✅ Done |
| 11 | Real-time Whiteboard | ✅ Done |
| 12 | JWT Auth Demo | ✅ Done |
| 13 | AI Chatbot UI | ✅ Done |
| 14 | Portfolio Dashboard | ✅ Done |

---

_Hit me with a PR if you spot a bug or have a better solution — happy to learn from you!_ 🙌
